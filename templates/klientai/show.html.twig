{% extends 'base.html.twig' %}

{% block title %}
	{{klientas.vardas}} {{klientas.pavarde}}
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-12">
			<h2 class="text-center">{{klientas.vardas}} {{klientas.pavarde}}</h2>
			<h3 class="text-center">{{klientas.gatve}}, {{klientas.miestas}}</h3>
			<h3 class="text-center">{{klientas.telNr}}</h3>
            <h3 class="text-center">{{klientas.elPastas}}</h3>
		</div>

	</div>
{% endblock %}
