{% extends 'base.html.twig' %}

{% block title %}Klientas{% endblock %}

{% block body %}
    <h1>Klientas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ klienta.id }}</td>
            </tr>
            <tr>
                <th>Vardas</th>
                <td>{{ klienta.vardas }}</td>
            </tr>
            <tr>
                <th>Pavarde</th>
                <td>{{ klienta.pavarde }}</td>
            </tr>
            <tr>
                <th>TelNr</th>
                <td>{{ klienta.telNr }}</td>
            </tr>
            <tr>
                <th>ElPastas</th>
                <td>{{ klienta.elPastas }}</td>
            </tr>
            <tr>
                <th>Miestas</th>
                <td>{{ klienta.miestas }}</td>
            </tr>
            <tr>
                <th>Gatve</th>
                <td>{{ klienta.gatve }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('klientas_index') }}">back to list</a>

    <a href="{{ path('klientas_edit', {'id': klienta.id}) }}">edit</a>

    {{ include('klientas/_delete_form.html.twig') }}
{% endblock %}
